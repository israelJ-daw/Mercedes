/*Crea una expresión regular que reemplace todas las instancias de 
la palabra "malo" en un texto por "bueno". Ahora modifica tu código 
para que el usuario introduzca las palabras a reemplazar.
 */

let texto=prompt("Escribe su frase");
let texto2;

for (let i = 0; i < texto.length; i++) {
    if (texto=/[malo]/gmi){
        texto2=texto.substring(i,i+4);


        if (texto2.indexOf("  ")){
            
            

        }
    }
    
}